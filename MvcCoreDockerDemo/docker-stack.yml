version: "3"
services:

  docker-demo:
    build:
        context: .
        Dockerfile: ./Dockerfile
    deploy:
      replicas: 6
      update_config:
        parallelism: 2
        delay: 10s
      restart_policy:
        condition: on-failure